[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "annadb"
version = "0.1.0"
description = "AnnaDB — embedded AI agent memory engine"
requires-python = ">=3.8"

[tool.maturin]
features = []
module-name = "annadb._annadb"
